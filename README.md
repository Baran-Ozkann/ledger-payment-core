# Ledger Payment Core

A payment core built on a double-entry accounting ledger with idempotent transfers.

## Setup

```
docker compose up -d
```

## Running

```
./mvnw spring-boot:run
```

Run the tests:

```
./mvnw verify
```
